export interface getQuestionsTypes {
  page: number;
}

export interface getAnswerTypes {
  questionNo: string;
  pinNo: string;
}

export interface questionTypes {
  questionNo: string;
  questionName: string;
}
